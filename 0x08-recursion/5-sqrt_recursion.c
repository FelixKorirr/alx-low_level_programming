#include "main.h"

int define_sqrt(int a, int b);
int _sqrt_recursion(int n);

/**
 * define_sqrt - returns the natural square root of a number.
 *@n:number to get its square root
 @b: iterator
 * Return: square root.
 */
int define_sqrt(int a, int b)
{
if ((b * b) == a)
return (b);
}
