#include "main.h"

int is_divisible(int a, int num)
int is_prime_number(int n);

/**
 * is_divisible - checks if number is divisible
 *@a: number to be checked
 @num: divisor
 * Return: if divisible = 0
 * if not divisible = 1
 */
int is_divisible(int a, int num)
{
if (a % num == 0)
{
return (0);
}
}
