#include <stdio.h>
#include "main.h"

/**
 * main - program that prints name of the program
 * @argc: Argument count
 * @argv: array
 *
 * Return: 0
 * korir codes
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
